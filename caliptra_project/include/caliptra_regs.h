// Licensed under the Apache-2.0 license
#pragma once

/*
 * 이 헤더는 더 이상 사용하지 않습니다.
 *
 * Caliptra 레지스터 정의는 자동 생성된 헤더에 있습니다:
 *   caliptra-sw/registers/generated-src/caliptra_top_reg.h
 *
 * 빌드 시스템 include 경로:
 *   -I caliptra-sw/registers/generated-src
 *
 * 직접 레지스터 접근이 필요한 경우:
 *   #include <caliptra_top_reg.h>
 *
 * 일반적으로 SoC 코드는 레지스터에 직접 접근하지 않고
 * caliptra_api.h의 고수준 API를 사용해야 합니다.
 */

#error "caliptra_regs.h는 deprecated입니다. caliptra-sw/registers/generated-src/caliptra_top_reg.h를 사용하세요."
